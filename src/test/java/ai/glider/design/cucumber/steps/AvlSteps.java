package ai.glider.design.cucumber.steps;

import cucumber.api.java8.En;

/**
 * Step Definition Class for ContactDto.
 */
public class AvlSteps implements En {

    public AvlSteps() {
        Given("empty list", () -> {
        });
        When("user saves the new contact", () -> {
        });
        Then("the save 'CREATED'", () -> {

        });
    }

}
